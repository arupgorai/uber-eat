import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardWrap: {
    minHeight: 75,
    height: 75,
    padding: 10,
    alignItems: "center",
  },
  imgStyle: { width: 50, height: 40, resizeMode: "contain" },
  textStyle: { fontSize: 13, fontWeight: "800" },
  categoryListWrap: { backgroundColor: "#fff", marginTop: 5 },
});
