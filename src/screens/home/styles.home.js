import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#eee",
  },
  headerWrap: {
    backgroundColor: "white",
    padding: 15,
    paddingBottom: 75,
  },
});
